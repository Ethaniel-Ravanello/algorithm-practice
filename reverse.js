function reverse(arr) {
	let split = arr.toString().split("");
	let result = split.splice(0, 1).concat();
	let reverse = result.reverse();
	console.log(reverse);
	console.log(reverse);
}

reverse(-321);
